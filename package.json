{
  "name": "@tokenring-ai/serper",
  "version": "0.2.0",
  "description": "Google Search and News integration via the Serper.dev API.",
  "dependencies": {
    "@tokenring-ai/app": "0.2.0",
    "zod": "^4.3.6",
    "@tokenring-ai/agent": "0.2.0",
    "@tokenring-ai/websearch": "0.2.0",
    "@tokenring-ai/utility": "0.2.0"
  },
  "devDependencies": {
    "vitest": "^4.1.1",
    "@vitest/coverage-v8": "^4.1.1",
    "typescript": "^6.0.2"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "types": "./index.ts",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  }
}
