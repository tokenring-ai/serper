{
  "name": "@tokenring-ai/serper",
  "version": "0.2.0",
  "description": "Serper.dev integration for Token Ring (Google Search and News)",
  "dependencies": {
    "@tokenring-ai/app": "0.2.0",
    "zod": "catalog:",
    "@tokenring-ai/agent": "0.2.0",
    "@tokenring-ai/websearch": "0.2.0",
    "@tokenring-ai/utility": "0.2.0"
  },
  "devDependencies": {
    "vitest": "catalog:",
    "@vitest/coverage-v8": "^4.0.18",
    "typescript": "catalog:"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "types": "./index.ts",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  }
}
